# Optional: Set these here or configure via the web UI at /config
OAUTH_CLIENT_ID=your_client_id_here
OAUTH_CLIENT_SECRET=your_client_secret_here

# OAuth provider: github, google, microsoft, auth0, custom
OAUTH_PROVIDER=github

# For Railway deployment, set this to your Railway URL
# Example: https://your-app-name.railway.app/callback
REDIRECT_URI=http://localhost:5000/callback

# Optional: Custom scope (leave empty for provider defaults)
OAUTH_SCOPE=

# Optional: Custom provider URLs (only needed if OAUTH_PROVIDER=custom or auth0)
CUSTOM_AUTH_URL=https://your-auth-server.com/oauth/authorize
CUSTOM_TOKEN_URL=https://your-auth-server.com/oauth/token

# Optional: API identifier (audience) for providers that support it (auth0, custom)
OAUTH_AUDIENCE=

# Flask secret key — set this so sessions persist across restarts.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your_secret_key_here

# Port (Railway sets this automatically)
PORT=5000

# Debug mode (set to False in production)
DEBUG=False
